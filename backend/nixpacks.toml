[phases.setup]
nixPkgs = ["python313", "python313Packages.pip"]

[phases.install]
cmds = ["python3.13 -m pip install -r requirements.txt"]

[start]
cmd = "python3.13 -m uvicorn app.main:app --host 0.0.0.0 --port $PORT"
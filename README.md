# Cargo-Management-System
A logistics cargo management system with REST API (FastAPI), analytics dashboard (React), and route optimization (Dijkstra). Built on MySQL 8.0 with role-based access control, triggers, and stored procedures.
